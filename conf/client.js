'use strict'

module.exports = {
    database_directory: './storage',
    files_directory: './downloads',
    use_test_dc: false, //If set to true, the Telegram test environment will be used instead of the production environment. 
}